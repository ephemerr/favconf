#!/bin/bash
declare -r FAV=~/favconf
ln -sf $FAV/git/.gitignore          ~/
ln -sf $FAV/vim/.vimrc              ~/
ln -sf $FAV/vim/darktooth.vim       ~/.vim/after/plugin/darktooth.vim
ln -sf $FAV/xbk/xbindkeysrc         ~/.xbindkeysrc
ln -sf $FAV/zsh/.zshrc.local        ~/.zshrc.local
